# SA-01 / SA-02 requirement audit at user-requested handoff

**SA-01 incomplete. SA-02 incomplete.** No original requirement or roadmap item
is removed. See [handoff](HANDOFF.md) for the immediate reproducible blocker and
[manifest](qualification-manifest.json) for exact source/artifact separation.

The registry5 package has 635/635 provider tests, lint/build and six finite
installed phases passing. Current source is registry6 and has only a focused
checkpoint: nine tests, one automatic-analysis failure, zero errors/skips.
Neither result qualifies the uncompleted fresh installed/GUI path.

| Original requested requirement | Implemented evidence and unresolved obligation |
| --- | --- |
| Verify recorded baseline and preserve earlier work | All 161 source, 13 documentation, seven dependency hashes and 55 installed members matched. Original ZIP/receipts and unrelated working-tree changes retained. |
| Competing physical paths at one CPU PC | Raw and native tests cover repeated PCs across banks, conditional bank domains, banked data identities and nested restoration. Graph nodes retain state; physical storage, CPU offsets and data identities remain distinct. |
| Compare viable Ghidra representations | Real widened physical-space/pointer-adapter candidate tested with native fresh/reopen failures retained. Overlay limits, SLEIGH-hook attempts and ordinary entry-injection rejection recorded. Scoped pre-flow protocol works on actual native mechanism probes and production graphs. This does not evaluate every possible segmented design or close general SA-02. |
| Normal canonical and derived entry workflow | Context aliases, proven CALL routing and public canonical selection work in normal DecompInterface tests, including same-Program opposite-flag contexts and actual callee writes. Conditional header and premise comments prevent a selected view being presented as an all-input ABI. New normal-window GUI checks remain unrun. |
| Later calls and mapper effects | Ordinary/configured/manual calls, returning/restoring/constant policies, direct and supported indirect writes, physical fetch/read transcripts and register/flag/memory/SP effects implemented and tested. Original raw native vetoes are retained until graph transport proof discharges them. |
| Returns, loops and nonlocal control | Matched returns, deterministic loops, terminal per-context calls, real wrapper cleanup and exact-boundary multi-frame nonlocal retirement tested. Intermediate native propagation adds no second hardware pop. Unknown or exhausted premises remain unresolved. |
| Repeated configured-site domains | Still limited to one exact configuration per physical software-transfer site. Multiple ordinary callee contexts do not establish repeated same-site software transfers with different premises. Preserve this boundary in subsequent work. |
| Rooted missing-instruction discovery | Public fresh helper/callee/caller/continuation discovery and native tests pass; speculative decoding is isolated, failed finite candidates roll back, application commits exact extents without a sweep. Payload/data/boundary/labels/references and cancellation regressions retained. |
| Fresh installed closure | **Not qualified.** State campaign passes preparation/application/context checks, then normal analysis changes caller aliases and stale checks reject them. Runs2/3 retain errors and before/after inventories. Latest source's full-auto-analysis regression still fails. |
| Paired invalidation, mutation and reapplication | Unit/native checks preserve paired drift, same-interface data mutation, nested proof context and selection cancellation/staleness. Independent review found and fixed selection rebaselining and absent-option cache issues. Registry6 adds caller prototype/body dependencies, but new source needs full qualification. |
| Reviewed repairs and saved work | Existing finite reviewed repair/removal campaign passes all six processes on the registry5 artifact. New context convention and source redirect removal preserve unrelated edits in focused tests. Fresh state campaign removal/reopen phases have not executed successfully. |
| Language/schema/native transitions | No constructor, language1.0, register width, compiler ID or mapping-schema change. Additive explicit compiler protocol, optional composite native companion, ownership5/effects5/registry5 implemented; current source registry6 is an unqualified successor. Historical ownership2/3/4 missing-primary receipts remain conservative. |
| Actual saved old-provider compatibility | Genuine saved registry4 Program was opened by the original qualified artifact and captured read-only, including raw/fresh decoding and user inventories. New-provider migrate and separate reopen/check remain unrun. No synthetic version rewrite is called actual migration. |
| Native/architectural equivalence | Separate raw-vs-emitted execution checks include physical data, registers/flags/SP/PC, ordered selector writes and two live external return words; native high-pcode/callee store checks remain independent of post-CALL copied constants. Wider hardware and all-input coverage remain open. |
| Strict logging and no hidden failures | Native prototype failures, compile/test oracle failures, real integration failures and installed unexpected warnings/errors retained. No analyzer disabled, queue cancelled, failed entry removed or body padded to qualify a run. Tooling51 includes one explicit unconfigured Java-companion skip. |
| Independent review | Review identified transport, domain, ABI, ownership and race issues; source corrections and independent tests retained. Latest automatic-analysis failure and remaining registration/domain boundaries preclude milestone approval. |

SA-02 advances through state-qualified graph/alias transport, distinct physical
reads and a scoped native protocol. General memory aliasing/reference recovery,
RAM images, mapper breadth and asynchronous transitions remain open. SA-04 gains
only rooted software-call instruction prerequisites and preservation/cancellation
support; callbacks, script/table classification and mutable RAM-code discovery
are not completed. SA-03/05/06/07 and all M0–M10 scope remain retained.

The failed installed inventories include ordinary logical CPU READ references
(e.g. unmapped CPU4200), normal DATA→WRITE refinement, and the distinct harmful
thunk/fixup changes. The graph access ledger identifies physical reads, but these
inventories are not a claim that all native/listing data references have been
recovered as context-sensitive physical references.

The user's wrap-up request ends this implementation turn. It does not change the
original acceptance requirements or promote partial evidence into completion.
