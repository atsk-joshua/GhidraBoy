# SA-01 canonical integration and renewed qualification

SA-01 and SA-02 remain incomplete. This receipt continues the original production
integration requirements without replacing the earlier finite models or historical
failures. The retained baseline is `../sa01-production-20260907/HANDOFF.md`.

At start, all 160 source and 12 documentation hashes in its manifest matched,
as did package SHA256 a57eec84d1a1aa7463dcc8d665091d8ddfb3d22086f03e8791753cd86af66c42.
Branch/base remain integrate-ghigbc / 16752093dce0147afd28762a2f8918144ec571c2.
Extensive preexisting staged, unstaged and untracked work was preserved. No branch,
commit, push, publication, active installation or private original was changed.

## Integration behavior

- Canonical and execution entries are explicitly paired; either path requires
  both live receipts. Original canonicalResult decompile assertions remain.
- Canonical installed injection expands the physical call and validated finite
  continuation CFG. Stable scratch COPY anchors preserve local branch targets
  through nested native injection. An owned CALL_RETURN listing override prevents
  initial native decoding of payload/obsolete-bank suffixes. It represents the
  complete expansion, not architectural helper/callee nonreturn. Raw hardware
  pushes and function bodies remain intact; alias listing uses normal mapped flow.
- Initial conflicting CALL endpoints, including legacy far-call endpoints, reject
  without silently changing knowledge. Later direct window crossings and every
  consumed instruction byte's physical permissions are checked.
- Matched ordinary nested RETs authorize precise noReturn/CALL_RETURN repairs with
  public and saved original/applied metadata. Other native vetoes remain active.
- Normal decoder DATA-to-READ/WRITE refinement no longer falsely invalidates a
  view. Arbitrary DEFAULT, wrong-operand, and user edits remain protected.
- Parallel analysis can invalidate a proof in flight. Bounded whole-callback
  retries discard interrupted proofs, revalidate all premises/dependencies and
  paired ownership, and still reject stable stale state or exhausted retries.

Contracts: registry/effects 4, injection implementation 3, neutral implementation
2, execution-view 2, view ownership stamp 3; template/validation 3 and ownership
envelope 4 retained. Engine: 20260907-sa01-canonical. No language constructor,
CPU pointer, compiler ID, native companion or physical mapping schema changed.
Old executable registry records reject and require public remove/review/reapply;
unmatched old ownership stamps confer no new destructive authority.

## Retained failures and corrections

| Evidence | Observation / disposition |
| --- | --- |
| canonical-baseline.log | Sandbox Gradle cache lock rejected; environment issue |
| canonical-baseline-authorized.log / canonical-baseline.xml | Exact retained missing canonical registry site failure at unchanged assertion |
| focused-1.log | Checked IOException declaration compile error; corrected |
| focused-2.log | Canonical test passes; five adversarial semantic failures reproduced |
| focused-3/4 logs and XML | Exact initial refs/window fixes pass; overbroad continuation transport restriction and nested defaultfallthrough repair corrected |
| focused-5 log/XML | 91/91 focused, lint/build pass; predates later installed fixes |
| full-1 log/XML | 563/563 full, lint/build pass; predates later fixes |
| focused-6.log | Stronger canonical memory assertion exposes payload decoding and wrong address-zero write |
| canonical-tail-diagnostic logs | First debug attempt needs explicit options; next captures real native transport failure |
| canonical-tail-before-native.xml | Correct c200 STORE injected, but its deleted local sequence target falls back to original instruction flow; pinned flow.cc explains mechanism |
| focused-7.log | 15 targeted cases pass after stable anchors and terminal canonical representation |
| focused-8 log/XML | 94/94 focused, lint/build pass |
| full-2 log/XML | 566/566 full, lint/build pass; predates final retry/operand refinement |

Installed resumed runs are retained under
`../sa01-production-20260907/installed-runs/resume-run-N/`, separately from original
runs 1–5. Resumed run 1 is a test-script checked-exception compilation failure.
Runs 2–3 isolate exact decoder DATA-reference refinement invalidating the constant
view, with prepared/analyzed public-state snapshots. Run 4 passes all phase-0
semantic assertions but correctly fails an unresolved witness warning during
parallel mutation. Assertions and process exit zero did not override that failure.

`environment.json` retains the reverified JDK/Ghidra/native tuple. The JDK is
21.0.12.1, Ghidra 12.1.3, native companion 12.1.3+ghidraboy.switch-recovery.2,
mac_arm_64 SHA256 5b736c3e9236667d35a732f226c99f0014736b9fe506147886a7f15ccb94939a.
No stock macOS native result is claimed. The independent notes distinguish source
review from execution and preserve findings and subsequent corrections.

Final current artifacts and remaining requirement disposition belong in
[completion-audit.md](completion-audit.md). A successful finite campaign cannot
close original general same-CPU banking, later continuation-effect or fresh
instruction-discovery obligations.

## Current executed qualification

- focused-9: 94 tests, zero failures/errors/skips; lint/build passed.
- full-3: 566 tests, zero failures/errors/skips; lint/build passed.
- build-inputs-final: passed. Tooling: 41 tests run, one explicit unconfigured
  Java-companion skip; remaining tests passed.
- Installed resume-run-5: all six phases passed, zero unexpected errors/warnings;
  final saved edits and six retired mapped spaces verified after separate reopen.
- Exact package/installed member identities: qualification-manifest.json; all 55
  installed members match ZIP 0bbea54f5c497c3c586d55987e001dcdb61a8bdc1b06546ec1c9cdeb3d4719e3.
- Normal GUI verification: blocked by locked Mac. A verified disposable session
  is launched; no observed Decompiler window or GUI PASS is claimed.

All source tests and provider artifacts above precede only this source-only
receipt/audit update, which is intentionally outside the packaged allowlist.
Packaged contracts/decisions/roadmap remain those in the exact qualified ZIP.

Retained native C still contains Ghidra injection/inlining and unreachable-block
comments; they are explicitly dispositioned in the completion audit and independent
review. Zero unexpected process-log warnings is not a warning-free-C claim.
`verified-source.json` is the earlier full-1 snapshot; final source authority is
`qualification-manifest.json`.

Final packaging follow-through: resumed installed runs 6 and 7 also pass all six
phases with the same provider JAR. Archive changes after run 5 are documentation
only: current roadmap status and a source-only evidence link. The final archive
is a18bc013d93c2815d4e929e814e9ec18248eb357c7c185f060ac1ffbed5cdd98;
`final-package-installed-check.json` verifies all 55 members and current packaged
documentation. `package-link-check-final.json` has zero missing local targets;
the inherited broken link is preserved in the earlier failed link-check receipt.
A final backward-edge native regression also passes unchanged (`backward-edge.xml`).

Final full-4: 567 tests, zero failures/errors/skips; lint/build pass. This includes
the added backwards conditional edge regression, with unchanged provider/package
bits from installed run 7. The GUI manual-unlock request remains unanswered.

## Subsequent GUI verification

The locked-desktop blocker is now resolved. [Normal-window verification](gui-verification.md)
records six canonical/alias comparisons, physical callee navigation, and a two-entry
normal CodeBrowser reopen check. Visible semantics and retained diagnostic categories
match installed baselines. Exact wrapper/runtime/native identities and independent
artifact comparison are retained. SA-01/SA-02 remain incomplete for their original
broader requirements. Source-only evidence and roadmap status were updated after
the qualified artifact; provider/package bytes are unchanged.
