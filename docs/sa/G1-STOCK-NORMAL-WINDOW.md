# G1 stock normal-window disposition — 2026-09-12

**SEMANTIC_OR_LIFECYCLE_BLOCKED: driver active-action sequencing; G1 is not PASS.**
The bounded run stopped for master review. No product semantic defect or need for
an extension-side event fix was established. No further batch starts automatically.

The current-language `F1234.gb` fixture replaces the historical language-1 F12
input for this task only. Its verified consumed mutation is file E000 D3→E4.
Historical Programs and language/migration decisions remain untouched.

| Obligation | Executed result |
| --- | --- |
| Visible stock normal CodeBrowser/provider/controller | PASS; real desktop capture and passive displayed-result read |
| P0 cold and P1 warm relation | PASS; actual displayed HighFunctions satisfy all 256 inputs and ordered effects |
| P2 committed consumed-byte edit, old registration retained | PASS; real Program event and stable passive observations; explicit stale-registration error with no HighFunction/C |
| Shipped proof preview and refresh | Executed successfully in the confirming run |
| Ordinary window Refresh and corrected E4 result | BLOCKED at immediate action enablement; no P3 capture |
| Canonical navigation/display isolation | Not run |
| Q replacement, new generation/topology and historical rejection | Fixture prepared; window phases not run |
| P/Q/P switching and missing registration | Not run |
| Saved current P/Q, consumer release, process exit, first read-only reopen | Not qualified; no reopened captures |
| Checker sensitivity | Seven initial-witness mutants rejected; full/reopen sensitivity not executed |

The first exact-candidate attempt passed P2 but supplied a null Decompiler location
to the Tools action. A driver-only correction reads the actual Listing location.
The confirming attempt then completed proof preview/refresh but found ordinary
Refresh disabled before delivery of the proof-write Program event. The pinned
stock action requires `DecompileData.hasDecompileResults()`; its context is absent
while the stale error is displayed. The event arrived approximately 17 ms after
the enablement check. Whether waiting for that event/debounce recovers the window
was not measured. This is not evidence that normal post-refresh recovery fails.

The ready runner now waits for normal events after explicit proof refresh and
before attempting ordinary Refresh. That final support change is source-reviewed
and compilation-checked only, not GUI-qualified. P2's passive boundary is unchanged.
The observed error and both failed attempt identities are retained externally.
No disabled action was forced, no cache reset or alternate decompiler was used,
and no additional native retry followed the bounded confirming failure.

One clean local checkpoint passed 819 instances in 82 classes with no failures,
errors or skips, preserving all baseline multiplicities. Lint/build, seven
build-input checks and 86 tooling tests passed; one existing optional tooling
skip remains. The two optional legacy protocol cases were not provisioned or run.
Provider/SLA hashes remain the accepted stock identities. Only the user-facing
Tools script changes the extension's executable payload. The installed candidate
ZIP is `ee272247a60d1ff7d95733641a303bde587c60da8a4f93cc6b580c185d2da965`.
Subsequent documentation and ready-runner changes do not acquire GUI acceptance
from that checkpoint. See the [runner instructions](../../tools/g1/README.md) and
[evidence index](evidence-index.json) for exact artifacts and coverage.

The supplied hosted-CI observation remains separate: default tests passed, while
old-language installed/migration phases failed. No missing-companion diagnosis,
CI-green claim, migration, stock release, switch recovery or broader W4 claim is
made here. Accepted predecessor stock semantics and current-record persistence
remain accepted within their original scope.
