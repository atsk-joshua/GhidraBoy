# G1 normal-window continuation — 2026-09-12

**HARNESS_OR_CHECKER_BLOCKED. G1 is not PASS. STOP for master review.**

Executable candidate `7dfb576f2f8fe8e8995faf6dd66922569c86033c` consolidates
normal Listing context, post-proof observation, explicit toolbar action receipts,
common initial capture validation, authority-specific refusals and the amended
missing-registration action branches. No production code changed.

The exact candidate passes one clean test/lint/build checkpoint: all 819 instances
in 82 classes retained, zero failures/errors/skips. Build-input checks pass 7;
tooling passes 92 with one existing optional skip. Focused Java compilation and
six new tooling tests pass. The accepted predecessor initial fixture passes the
strengthened checker and all 11 sensitivity controls reject; this is explicitly
separate from the new capture below.

| Normal-window obligation | New evidence |
| --- | --- |
| P0/P1 | Displayed D3 relation passes all 256 inputs each; same result object reused |
| P2 | Correct visible stale-registration refusal, no HighFunction/C; actual revision-4 event precedes the later commit receipt |
| Explicit shipped proof actions | Real Listing context; revision 4→5 write and delivered event |
| Post-proof/pre-toolbar | Natural E4 recovery captured after bounded settlement, before any toolbar request |
| Actual ordinary Refresh/P3 | Requested, enabled, invoked and dispatch completed; new displayed result object, E4 all-input relation passes |
| Canonical isolation | Source snapshot unchanged; canonical displayed C lacks conditional header |
| Q I1 | Six frame/carry cases produce C074=31 |
| Physical I2 replacement/Q1 | Old image/proof retained; passive specific refusal with correct identity |
| Explicit generation 2 | Establish/install returned, history and new carrier recorded; no settled Q2 capture |
| Q2 A7, historical rejection, switching | Unrun after immediate transaction-guard stop |
| Saved positive state, missing registration, qualified closure/reopen | Unrun; neither negative-action branch executed |

The first-session log reports `Outer transaction conceals real event boundary`
from `drain()` called by Q2 navigation. The transaction owner and lifetime were
not captured. The guard does not establish a settled product failure, so no
extension correction was justified or applied. No second GUI attempt followed.
The JVM exited with code zero through error cleanup, without a tool-closure marker;
this is not qualifying closure and no second JVM was launched.

The initial/full checker returns `no real event delivery`: its strict receipt-order
predicate expects the P mutation event after the commit marker, but the real event
arrived 125708 monotonic nanoseconds before that marker. The original evidence and
checker result are retained. Per-record integrity and semantic replay are reported
separately, without rewriting the timeline or claiming workflow acceptance. Full
sensitivity requires a complete passing capture and remains unrun, including the
attempted-without-dispatch and prohibited-P3-navigation controls.

The stock runtime matches all 5218 pinned release files and the exact extension.
Provider/native/SLA bytes are unchanged. No private reset/interface, production
lifecycle correction, remote write, active installation change, migration, G2/G3
campaign or stock-release claim is included. The shared G1 cap was not reset.
See the [evidence index](evidence-index.json) for the report, archive and replay.
