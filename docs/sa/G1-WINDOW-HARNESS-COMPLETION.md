# G1 window harness completion — 2026-09-12

**Scoped G1 workflow PASS; STOP for master review. No stock-release claim.**

The corrected measurement protocol records source-qualified Program events,
operation/subtransaction/encompassing-transaction identities and commit-call
entry/return. It accepts either valid callback/receipt order, while requiring a
completed commit, relevant range/option event, stable target state and no active
intervention. Unknown transactions are observed, never ended by the driver.

Q2's new record identifies a transient `Auto Analysis` transaction. It settles
normally and the actual window produces A7. This does not retrospectively identify
the preceding run's uncaptured transaction or its writer thread. No production
semantic or lifecycle correction was made.

| Final obligation | Result |
| --- | --- |
| P0/P1 | D3 relation over all 256 inputs; same displayed result reused |
| P2 | Genuine passive, source-specific stale-registration refusal |
| Natural post-proof / actual Refresh / P3 | Natural E4, then enabled/dispatched ordinary Refresh and fresh E4 result |
| Canonical source/display isolation | Preserved |
| Q0 / physical replacement / Q1 | I1/31, retained old authority, passive replacement refusal |
| Q2 / historical generation | Current A7 six-case relation; old generation specifically refused |
| P/Q/P and quick alternation | Correct settled Program/Function associations |
| Disposable negative | Explicitly prepared copy, warm positive, passive refusal, unavailable Refresh then labelled ordinary away/back fresh refusal |
| Save, actual close and second JVM | All consumers released; both JVMs exit zero with tool closure markers |
| Immutable reopen | First P/current-Q/old-Q results correct; complete saved authority values preserved |
| Filesystem comparison | Six exact index/journal checkpoint changes; all other project files byte-identical |
| Checker sensitivity | Complete passing base; all 28 initial/full/protocol controls reject |

One intended clean checkpoint passes all 819 default instances in 82 classes,
with no failures/errors/skips and identical case multiplicities. Build-input and
tooling suites pass, with one existing optional tooling skip. The first post-build
workflow passed capture checks but had occluded desktop screenshots; it remains
nonqualifying. A bounded launcher-only visibility correction was compile-tested,
reviewed and confirmed with fresh fixtures and the same tested product payload.
No second clean build or product correction was used.

The confirmed executable support candidate is
`96119bf74257e323a1377baf76607f70d98c96df`, tree
`6b832ecb2a554649d8558846739204e13145a0c0`. The extension under qualification is
`b83d3781b70294c61424a4fbe9f7e4d25b5482aad1007915d1d94af8052990b0`.
Final documentation is separate from that frozen archive and executable source.
Rehearsals, failed attempts and predecessor evidence are retained separately.
See the [evidence index](evidence-index.json) for exact identities and replay.

The shared G1 investigation cap was not reset. The production-correction allowance
was unused. The enabled-Refresh negative alternative was not forced; actual Refresh
was mandatory and executed for P3. Other gates, migration, switches, broader W4,
platform/CI qualification and stock release remain outside this result. The next
obligation is master review; no automatic follow-on campaign is authorized.
