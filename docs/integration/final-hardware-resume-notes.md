# Final source hardware/accuracy reconciliation

2026-09-06, macOS 26.5.2 arm64. This audit addresses the original M7 hardware and accuracy exit checks in `docs/debugger-integration-plan.md:278–292`; CPU-01 and installed/release acceptance remain separate coordinator work. No tracked files, package, index, branch or commit were changed by this worker. All new execution output is isolated under `dist/integration-resume/hardware` and `/private/tmp/ghidraboy-hardware-final`.

The five audited gates have evidence for their original named assertions within the matrix below. Recommended disposition is **PASS for bounded source qualification**, with **ACCURACY-02 comparison status DIFFERENCES_RECORDED (exit 2)** retained verbatim. This is completion of the prescribed comparison/classification process, not a claim of timing equivalence. No genuinely unfulfilled named non-Deck assertion was found in these five gate definitions after the fresh geometry and guest-bus regressions. The broader qualification exclusions below remain exclusions; these results alone do not establish Release B, all model/revision/device combinations, or installed acceptance.

## Gate-to-evidence matrix

| Gate and exact requirement | Actual tests and current evidence | Model / boot policy / scope disposition |
| --- | --- | --- |
| **HW-01**: boot/model selection and actual reported configuration agree; reject incompatible boot images and silent model fallback. Exercise true DMG and both CGB modes independently. | `test_models_mbc2.py`: `test_actual_dmg_hardware_boot_and_banked_execution`, `test_cgb_native_and_compatibility_modes_restore_across_boot`, `test_mismatched_boot_and_old_native_library_fail_explicitly`, coherent hardware/snapshot and bounds tests. Native final run passed all named cases. `test_mapper_geometry.py::test_detected_controller_fallback_is_rejected` also rejects header/core controller disagreement. | **PASS, SameBoy DMG-B native DMG; CGB-E DMG-on-CGB; CGB-E native CGB.** Real redistributable boot execution and actual post-boot model/mode are checked. CGB 2,304-byte versus DMG 256-byte boot mismatch and invalid native model are rejected. The independent matrix does not test original Nintendo boot startup register/divider values. |
| **HW-02**: ROM-only/MBC1/MBC2/MBC3/MBC5 advertised geometries pass selector/alias/boundary tests, including MBC2 nibble/mirror behavior, MBC5 bank 256, MBC1 lower-window changes, SVBK zero and echo RAM. Unimplemented wiring remains unknown/unsupported. | `test_mapper_geometry.py`: **56 ROM geometry/model rows**, **14 RAM geometries**, last physical byte/bank bounds, disabled cart visibility, bank-zero aliases, SVBK 0→1 and all WRAM selectors with echo, two separate VRAM banks, detected-controller fallback rejection. `test_address_edges.py`: MBC5 bank 256 executes correct physical ROM bytes; MBC1 ROM0 changes to bank 32; writer provenance retains remapped ROM0 and echo execution identity; selected MBC3 RTC remains distinct/unknown. `test_models_mbc2.py`: both types 05/06 and models, A8 control decode, bank-zero remap, RAM enable/disable, 512 low-nibble cells and CPU high-F/mirror behavior, attempted byte versus physical before/after, changed-value predicate, bounds/checkpoint. | **PASS for standard advertised mapper geometries, not special wiring.** ROM rows are ROM-only 2; MBC1 powers of 2 through 128; MBC2 through 16; MBC3 through 128; MBC5 through 512, each on DMG-B/CGB-E. RAM is MBC1/MBC3 2/8/32KiB; MBC5 2/8/32/64/128KiB; rumble 8/32/64KiB. Geometry tests unmap boot directly and exercise adapter/selector behavior; separately executed boot/model tests establish operating modes. Unimplemented mapper headers and detected core fallbacks are rejected; RTC-selected CPU bytes are not asserted as SRAM. MBC1 multicart and other special wiring are not newly advertised by these tests. |
| **HW-03**: applicable hardware ROM suites exercise timers, interrupts/EI delay, HALT/STOP, bus restrictions, boot unmapping, CGB speed switching and DMA/HDMA behavior. A correct engine result does not prove the debugger's event attribution is correct; check those separately. | Fresh pinned Mooneye rows cover TIMA reload/write, TMA write, DIV write/rapid toggle; EI sequence/timing, rapid DI/EI, IE push; HALT cases; OAM DMA basic/register/start/restart/timing and DMG source cases. Native tests separately cover STOP blocked boundary, no false idle-HALT breakpoint, post-speed-switch wake fetch; actual boot unmapping; `test_cgb_devices.py` KEY1/STOP speed and time units, general/HBlank DMA copies/status/masking and **mode3 guest CPU VRAM/OAM read lockouts**. Bus case checks attempted VRAM0 byte 0x99 with physical before/after 0x44; earlier `test_blocked_vram_attempt` checks exact writer PC. DMA/HDMA tests require no false CPU-watch attribution; interrupt entry test requires no CPU writer. Zero event losses checked in device cases. | **PASS for named exercised subsystems.** Independent Mooneye expectations use true DMG-B and CGB-E in DMG compatibility; native-CGB STOP/speed/HDMA/bus coverage is explicitly authored regression evidence, with KEY1/HDMA expectations attributed to pinned Pan Docs. Exact DMA stalls, every PPU/APU/bus case, native-CGB independent ROM-suite coverage and physical measurements are not claimed. Hardware effect and debugger event assertions are separate. |
| **ACCURACY-01**: a debug-instrumented run matches a pristine-core run for comparable defined state/output; nondestructive inspection leaves engine state unchanged; instrumentation patches cannot validate themselves solely through their own event output. | Fresh `mapper-accuracy/report.json`: **55 rows ×3 variants=165 PASS**, all streams equal, **3,986 inspected serialized-state boundaries unchanged**. Same pinned upstream source exported into pristine and instrumented builds; pristine uses direct `GB_run` without GhidraBoy callbacks. Current adapter frozen into instrumented probe; independent signatures/state/output compared. Fresh isolated authored speed-switch fixture: **3 variants PASS**, **73 more unchanged inspection boundaries**, each 1,170,457 calls  / 26,302,792 ticks. Two comparator negative tests pass (changed late byte exact offset; missing/truncated stream rejection). | **PASS for selected fixtures and sampled boundaries.** Matched model/ROM/boot bytes, seed, released buttons, accurate RTC policy (no RTC mapper cases), run-call sampling and final signature. Registers/flags/ticks/banks/physical memory/IO/pixels compared. Seeded undefined startup bytes are only same-core observability evidence. Published expectations are not derived from the instrumented event stream. Total sampled inspections including speed=4,059, not continuous/exhaustive observation proof. |
| **ACCURACY-02**: cross-backend fixtures normalize model/boot/clock/input and compare defined registers/memory at explicit synchronization boundaries. Differences produce minimal reproducers and classified explanations, never majority-vote correctness or blanket suppression. | Fresh `hardware/cross-backend/comparison.json`: defined registers/banks and initialized CPU/physical RAM **match 12 named physical-PC boundaries**, including PC 0x4000 in banks1/2. **10 of 11 exact normalized timing intervals match**. Five verifier tests PASS. Negative-control changed ROM operand produces exactly 3 expected AF/WRAM differences. Real speed-switch mismatch remains classified `speed-switch-device-delay-unresolved`, reproducible bytes `10 00 00` from PC 0x0284 to0x0287, SameBoy 131,096 versus mGBA 20 ticks at 8,388,608Hz. Tool exit remains **2**, `DIFFERENCES_RECORDED`. | **Comparison/classification requirement satisfied for an explicitly initialized native-CGB subset; timing equivalence remains unqualified.** SameBoy CGB-E executes a 2,304-byte redistributable boot; mGBA CGB uses engine-post-boot/no BIOS. These policies and silicon revisions remain distinct, never described as equal. Fixture normalizes defined registers/RAM, banks, keys, LCD/timer/interrupt controls at initial physical PC 0x0250; startup/uninitialized bytes/PPU/APU/IO/absolute startup ticks excluded. Durations normalized by exact tick-frequency cross multiplication, not rounded tolerance. Source mechanism retained; no physical adjudication. |

## Fresh execution receipts

The already-completed coordinator runs were verified rather than repeated: native-final **110 tests PASS**, child PID 52308 / wrapper 52305, exit 0; mapper-accuracy child 49670 / wrapper 49667, exit 0. Their receipt/log hashes and every recorded input match current files. Frozen accuracy adapter, header, probe source, both executables and both static libraries match their own report identities. This is recorded in `verification.json`.

The worker runner `run.py` records commands, cwd, wrapper/child PIDs, start/end, bounded watchdog, actual exit, log hash and input-receipt hash. No source dependency changed during these executions (`input-drift.json` is empty). `setup.json` records unchanged original scripts and the sole speed-script adaptation: new output path; pristine/instrumented probe bytes were copied from the fresh mapper-accuracy build and retain its hashes. Baseline receipts were untouched.

| Refreshed check | Exit / result | Child PID | Receipt (under `/private/tmp/ghidraboy-hardware-final`) |
| --- | --- | ---: | --- |
| legacy | 0 / PASS | 53671 | `legacy.json` + corresponding log |
| speed-pristine | 0 / PASS | 53670 | `speed-pristine.json` + corresponding log |
| accuracy-verifier | 0 / PASS | 53672 | `accuracy-verifier.json` + corresponding log |
| cross-verifier | 0 / PASS | 53679 | `cross-verifier.json` + corresponding log |
| cross-backend | 2 / DIFFERENCES_RECORDED | 53680 | `cross-backend.json` + corresponding log |
| private-parity-final | 0 / PASS | 53858 | `private-parity-final.json` + corresponding log |

The genuine M5 restore uses the original retained metadata/state bytes (schema 2 old patch a3526007…), not a newly fabricated envelope: metadata SHA256 `2dc13b227210d3b03963e8bba5c0a3dbecbf8d04740468b8a972d6a29409eb35`, state `ac7e24840483e3e9bc0eead31d1fae9512f596dd5853fb98a5b6258b5ebd4089`. It restores before and after a pending speed-switch fetch; current core section 160 versus legacy 152 bytes, pending marker clears, PC 0x4567/ROMX 2/BC 0x3456 restored and complete reserialized state equals the reference. Legacy compatibility remains one-way, exact old schema/model/patch only.

The optional private consumer's existing parity check was refreshed against the
current native library. Detailed game-specific outcomes and provenance belong
to GhiBW3's private `docs/integration/final-consumer-1fce58f.md`; no new battle
claim is part of generic hardware qualification.

## Dependency and result hashes

`inputs.json` captures all debugger Python/tests, relevant accuracy tools, native/header/patch/library/boot/fixture identities before execution. `private-parity-inputs.json` adds consumer dependencies. The machine-readable `verification.json` and per-command JSONs are copied alongside this report under ignored `dist/integration-resume/hardware/receipts` for coordinator use. Full commands and output paths remain in those JSONs.

| Path | SHA256 |
| --- | --- |
| `debugger/backends/sameboy/native/ghigbc.c` | `9655797cc1a8f7f19e7dcfd7ed645e275e214e726f9cba556f9b8727be2dd15a` |
| `debugger/backends/sameboy/native/ghigbc.h` | `0f52aa0792eba6065d0fa9db68febc85435fd9f273fad3fa3d43542177e3219d` |
| `debugger/backends/sameboy/native/patches/0001-cpu-bus-provenance.patch` | `afb55300828df629058e9fde30024801705cb4ffc3c935e58c5720da5d217b67` |
| `debugger/build/libghigbc.dylib` | `ba30da5996633003cbcf26ab528aa2f6bee1c774c97f5db194c1ddefd2714f49` |
| `debugger/build/libghigbc_mgba.dylib` | `9c0b84e7bd678e695950bbf5451aa916d6b2d6e7c55f08327d6659ff8ac675cf` |
| `debugger/tests/test_mapper_geometry.py` | `837949d22a561344980db2627751c083f57fd903acd9e1334d98506d7e523bc7` |
| `debugger/tests/test_cgb_devices.py` | `a12648ee21fa16a13c822efde5bcfdb6a260625dbdc7c6f099aa0ed99a2d8a1a` |
| `tools/dependencies.json` | `ddb06434f58a82b0ac9d652597049cd9586326e152fde24b0ca13756c86fd72f` |
| `tools/accuracy_sameboy.py` | `cdda7119b109f70ebb51fa86155cc2eec8760afd9de7ea2111c07f3dd478b9a3` |
| `tools/compare_backends.py` | `0f08736fc46b2305ef7bbcc63c95979e6ea583263cc48ccba32c6a69cabf5f86` |
| `dist/integration-resume/native-final/receipt.json` | `1ee0f7baae12f50918c1a15c0c83fb2e8da8c9839e8ef36181537e312f4f488d` |
| `dist/integration-resume/mapper-accuracy-command/receipt.json` | `2520f1653e5f2e2cf6aba16f027b16cddd6ce71666c1f4d0ae97788be5d58150` |
| `dist/integration-resume/mapper-accuracy/report.json` | `38fe03093cab31a86adfd3cb37d1761b1710ceaa960b97c8a516f68638053e54` |
| `dist/integration-resume/hardware/speed-pristine/report.json` | `c95b6cc030d07e366a2d61f95e1828e9194a065407623dc8807e49ac3de4a5fa` |
| `dist/integration-resume/hardware/legacy/report.json` | `d23d20e24f5660ed46c8a379eb26115d3ff7f46a86c5eed8f5e9c716db95de75` |
| `dist/integration-resume/hardware/cross-backend/comparison.json` | `234bd352eb6bd97b6eae7ff52d86dc586dbc2eda334cac9e888b6787ac77fb2c` |

The canonical SameBoy patch remains afb553…; the current native source delta is constructor rejection when the core detects a different mapper than its header identity. Pristine speed comparison uses rebuilt current frozen adapter dependencies, and cross-backend/legacy/private checks use current production library ba30da…; old native hashes in historical notes are not silently promoted.

## Scope reconciliation and retained unknowns

The original gates require named subsystem exercises, explicit model/boot scope, independent pristine comparison, and classification of real cross-backend differences. They do not require a universal all-PPU/APU/native-CGB independent ROM suite before these bounded assertions can be recorded. The selected Mooneye manifest preserves 36 acceptance rows (19 DMG / 17 CGB compatibility), 2 CGB misc rows, 14 emulator-only MBC2 rows, and 3 authored model rows. Mooneye revision 31510e12eea6286d36eea060a6adde755e1067aa and its WLA-DX CI revision 89a90a56be5c2b8cf19a9afa3e1b32384ddb1a97 are pinned; external provenance and classifications stay visible. Device expectations cite Pan Docs revision fe246067b695b5404a4a6a47efb4fd6d921ececb, retrieved 2026-09-06 in the existing device note.

No local physical GB/GBC measurements exist; this is UNVERIFIED, consistent with HW-04. Cross-core STOP timing, original boot startup truth, unimplemented mapper wiring/RTC observations, complete PPU/APU behavior, all silicon revisions, broader mGBA support and physical Steam Deck remain unqualified. Installed macOS/Linux acceptance, lifecycle/resource, static CPU, packaging, performance and Release B gates must use their separately refreshed evidence. This worker did not rerun or approve those gates.
