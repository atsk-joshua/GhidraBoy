# Copy into the next task

```text
/goal Finish the GhiGBC → GhidraBoy migration as master architect and coordinator. Start with /Users/joshuahansen/dev/GhidraBoy/docs/integration/agent-handoff.md and handoff-state.json, then read only relevant sections of docs/debugger-integration-plan.md and status.json.

Complete all remaining non-Deck work; the user explicitly deferred final physical Steam Deck verification. Preserve the original acceptance gates, compatibility, architecture boundaries and declared budgets. Keep mGBA's actual capabilities/unknowns and cross-backend timing differences honest.

First inspect the completed soak reports and collect any remaining cleanup without restarting blindly. Investigate the mGBA closed-trace AWT failure and missing uncaught-error accounting before trusting any terminal PASS. Then finish final-artifact GUI workflows, external GhiBW3 verification, required release checks, documentation/cutover and coherent local commits. Reuse passing evidence only through explicit source/artifact dependency matching; rerun affected checks after fixes. No remote publication, repository archival or removal of compatibility delegates.

Use up to three bounded workers alongside useful coordinator work. Prefer fresh fork_turns="none" agents with packets containing one objective, owned files, exact inputs, tests and report path. Avoid full-history forks and broad context dumps; retire workers after each concrete chunk. Coordinator alone owns shared progress/status/handoff and physical UI. Serialize shared packaging; isolate test homes/runtime copies. Persist long-job commands, hashes, PIDs, logs and exit codes. Preserve unrelated files and existing work; do not prefix branches with codex/. Continue until the agreed non-Deck outcome is proven, or document a concrete external blocker without claiming completion.
```

The handoff supplies exact runtime, evidence, environment and ownership details.
