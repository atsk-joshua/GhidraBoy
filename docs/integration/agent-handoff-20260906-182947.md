# GhiGBC → GhidraBoy agent handoff

> Historical handoff snapshot. Implementation has resumed with user-authorized parallel agents; see [active work](active-work.md), [progress](progress.md), and the updated plan checkpoint. Preserve the original source/evidence identities below.

Prepared 2026-09-06. Start with the [plan's execution checkpoint](../debugger-integration-plan.md), [execution ledger](progress.md), [58-gate register](status.json), and [handoff state snapshot](handoff-state-20260906-182947.json). This report records completed work and unfinished implementation; it is not a release receipt. The current request is to update documentation and hand off, not to publish, archive repositories or start another task.

## Scope and decisions to preserve

Build a first-class GB/GBC analysis, debugging and research tool. Teaching workflows, lessons and educational exercises are explicitly excluded. Documentation is operational/reference material; synthetic ROMs are regression fixtures. Legacy names such as `teaching.gbc`, `StudyProvider` and `ghigbc` remain where compatibility requires them; their presence does not add teaching scope.

GhidraBoy owns the static SM83 provider and optional generic debugger. SameBoy remains the reference backend with full existing capability preservation; mGBA is the next embedded backend. Other engines require a demonstrated unmet use case and the same feasibility/conformance gates. GhiBW3 remains an external optional consumer. Keep the current Java/Python/native boundaries, immutable observations, explicit unknowns, accurate event provenance and engine-specific checkpoint payloads. Avoid a new generic plugin framework or second IDE.

Release A requires M0–M6; Release B adds M7–M10. Neither is qualified. A component test PASS applies only to its named source/artifact snapshot. Preserve the full gate set and distinguish incomplete, failed and blocked checks. Physical GUI/Steam Deck acceptance cannot be replaced by a headless or virtual-display result.

The user says not to prefix branches with `codex/`. Do not spawn agents unless explicitly requested. Preserve unrelated files and existing staged work. This documentation handoff does not authorize remote publication or repository archival.

## Repository state

| Checkout | Branch / HEAD | Role |
| --- | --- | --- |
| `/Users/joshuahansen/dev/GhidraBoy` | `integrate-ghigbc` / `d78da47` | Main implementation; contains staged and unstaged work below |
| `/Users/joshuahansen/dev/GhiBW3` | `integrated-debugger-consumer` / `c8a858f` | Consumer migration committed separately |
| `/Users/joshuahansen/dev/GhiGBC` | HEAD `c1cfeef` | Preserved original source/history |

Do not stage or remove unrelated GhidraBoy `docs/changes-since-official-release-report.md`, GhiBW3 `.DS_Store` or `docs/integration/GBW3_DECOMPILATION_AGENT_HANDOFF.md`, or GhiGBC `docs/STUDENT_GUIDE.md` and `output/`. Old branches, source bundles, private assets and rollback artifacts are preserved. No remote writes occurred in this handoff.

Important committed changes, in implementation order:

| Commit | Work |
| --- | --- |
| `b73d33b` | Baseline, preservation checks and backend probes |
| `7ab8950` | History-preserving import; original GhiGBC commit is a merge parent; 83 selected files |
| `91062e7` | Optional root Gradle build and verified artifact manifest |
| `7e178c4` | Isolated external debugger probe and startup diagnostics |
| `3d27f5b` | Canonical dependencies, standalone macOS packaging |
| `67a9db4` | Linux packaging/SDL qualification |
| `3905c82` | Initial backend protocol and SameBoy isolation |
| `c55a2a9`, `d77e93d` | Shared session/recovery ownership and compatibility evidence |
| `d78da47` | Real process-failure lifecycle and saved-work/consumer checks |
| GhiBW3 `c8a858f` | Consume integrated artifacts without requiring the old GhiGBC checkout |

## Verified work

M2/M3 and M5 are complete at the source snapshots in their receipts. M0 inventories and preservation checks are recorded PASS, but the plan's fuller performance baseline remains incomplete. M1's four gates remain in progress. M4's code and failure checks pass; `BACKEND-04` remains blocked by the last observed locked desktop. M6–M10 have no completed release qualification.

Recorded evidence includes:

- Static provider: 456 tests, 21,000 pinned external vectors, 501 persisted instruction-constructor checks, copied current Program preservation and actual 11.3.1 → 12.1.3 database migration.
- Build/install: isolated clean static and full builds, paths containing spaces, canonical dependency verification, byte-reproducible macOS package, Linux native/runtime packaging and offline extracted-package checks. Installer recovery/rollback has 11 generic checks and 13 optional composition checks at the recorded snapshots.
- Backend/session: 69 native/Python tests plus 7 build/architecture checks at M4; generic code has no native handles; ordered bounded queues, urgent pause, stale contexts, overflow, partial-edit recovery and verified checkpoint copies are tested. Eleven before/after state/memory/event records match the earlier implementation. Legacy schema-2 SameBoy state restores.
- Real Trace RMI: Mac/Linux runs exercise 250 captures, mappings, edits, checkpoints and history. Forced owned-sidecar death, disconnect, bounded exit, replacement-session identity and stale-action rejection pass.
- Persistence/consumer: four copied trace fixtures reopen in independent Java-only processes without an emulator or profile. GhiBW3 static isolation and live service linkage pass. Exact-revision private consumer parity records 53 unit templates, 33 weapon templates, 100 captured units and 1,623 fields; no ROM bytes are packaged and no new battle-replay claim is made.

Use [M3](m3-evidence.json), [M3 Linux](m3-linux-evidence.json), [M4 boundary](m4-boundary-evidence.json), [M4 session](m4-session-evidence.json), and [M5](m5-evidence.json) receipts for exact commands and identities. The existing Ghidra terminal-close diagnostic is retained in logs despite successful explicit async-error/termination assertions; do not hide it or infer every new diagnostic is benign.

## Uncommitted M6 work

`debugger/tests/ghidra/RealTraceTest.java` was already staged before this handoff. It adds `--metrics`, `--soak`, and `--legacy-baseline`:

- Metrics exclude five warmups and record 20 measured step/pause cycles, separately measuring reply and capture-ready latency. Capture-ready polling is 5 ms without the old artificial settling delay.
- Soak runs at least 1,800 seconds and 100 checkpoint/restore cycles, with 15-second execution intervals, five-step batches, separate process-failure/replacement scenarios at cycles 30/60, and per-cycle RSS/thread/handle/storage/pause/event observations.
- `--legacy-baseline` is intended to exercise an older pre-backend runtime using the same measurement harness. Its baseline comparison runs have not been validated yet.

Untracked `tools/performance_limits.json` declares budgets before the extended run; `tools/assess_performance.py` assesses matched run p95 and bounded resource growth. The compiled/package harness log is `dist/integration-baseline-20260906/m6-harness-build.log`; the frozen macOS archive SHA256 is `b2c8dac88ba6fe327c8af7bb6b67d96d9dbfe1e1f777a1a8358e3c2e9fdfce69`. This package predates the uncommitted hardware-model edits.

The assessor is unfinished/unvalidated. Review its input validation, including missing identity fields (currently two missing values compare equal), non-finite samples and trusting supplied p95 values. Review trace-storage accounting across session replacements and the distinction between the primary target and separate lifecycle scenarios. First/last resource windows alone do not establish steady-state behavior or post-close cleanup. Do not relax predeclared limits in response to an unfavorable result.

### Completed soak: assess before rerunning

The existing run completed during this handoff. The original tool session returned **exit 0**, and the log ends in `REAL_TRACE_TEST_PASSED`. The report contains **109 cycles / 1,802.203868 seconds**, maximum measured soak pause **65.041667 ms** and zero reported dropped events. Previously observed shell PID `77376`, Java PID `77400` and primary agent PID `77503` were absent in the post-completion process query. This is not a full descendant/resource leak audit. Exact report/log hashes are in [handoff-state.json](handoff-state-20260906-182947.json).

- Original tool session: `88166`, collected as completed with exit 0 during handoff. No need to resume or restart it; verify retained report identities before using them.
- Log: `/Users/joshuahansen/dev/GhidraBoy/dist/integration-baseline-20260906/m6-soak.log`.
- Reports: the same evidence root's `m6-soak/session-performance.json`, `session-soak.json`, and `declared-limits.json`.
- Frozen runtime: `/private/tmp/ghidraboy-integration-20260906/m6-qualification/GhidraBoy-Debugger-20260905-integration1-macos-arm64`.
- Isolated user home: `/private/tmp/ghidraboy-integration-20260906/m6-soak-home`.
- Invocation in the frozen runtime: `bash scripts/test_ghidra.sh --metrics --soak`, with the JDK/Ghidra paths below and `GBC_TEST_HOME` / `GBC_EVIDENCE_DIR` set to these isolated locations.

The report uses `RUNNING` and `COMPLETED_MEASUREMENTS`; the latter alone is not an assessment PASS or proof the whole Java harness exited successfully. Here the exit/log result was separately collected, but the budget assessor has not been validated or run. The log includes intermittent stale static-binding session/epoch warnings; inspect whether retry completes and whether mapping correctness is retained. No new soak or background job was started for this documentation request.

Remaining M6 work: at least three comparable baseline and three candidate runs for all four metrics; validated budget assessment and full trends/cleanup review; physical GUI; final source/artifact-matched static, runtime, installer, reproducibility and platform acceptance. Older settling-delay numbers are not interchangeable with the new capture-ready measurements.

## Uncommitted M7 work

Modified native files: `debugger/backends/sameboy/native/ghigbc.c` and `.h`. Modified Python files: `debugger/python/ghigbc/backend.py`, `agent.py`, `backends/sameboy.py`, and `debugger/tests/test_backend.py`.

The draft adds `gc_create_model_buffers` and `gc_get_hardware`, a six-field hardware descriptor and explicit CGB-E/DMG-B selection. Existing creation entry points retain CGB-E behavior and the existing `gc_state` ABI layout is unchanged. Python selects 0x900-byte CGB or 0x100-byte DMG boot assets, checks actual native model, captures actual mode and RAM sizes, and limits exposed banks accordingly. Boot ranges move into the snapshot interface; the agent no longer assumes the second CGB boot window exists on DMG. Original CGB schema-2 checkpoint identities are retained; DMG uses schema 3.

The latest native build log contains no compiler diagnostic, and `m7-model-baseline-tests.log` explicitly reports **69 tests passed**. This is existing-suite regression evidence only. The existing `test_dmg.py` previously exercised a DMG-header cartridge on CGB hardware; its name does not prove the new native model. The prior shell invocation did not retain independent structured exit statuses for both commands. Source hashes and log hashes are captured in the state snapshot; no dedicated M7 release receipt exists.

Next implementation checks:

1. Test an actual `model='DMG-B'` instance: selected versus observed model, 8 KiB WRAM/VRAM, two WRAM banks/one VRAM bank, boot coverage and banked execution, incompatible boot rejection and no silent fallback.
2. Test CGB native and DMG-on-CGB mode through boot and capture transitions. Mode can change during boot; keep checkpoint configuration identity stable and observations truthful. Review the generic schema-3 session identity before applying it to mGBA; it currently includes mode.
3. Review old-native-library fallback semantics, coherent state/hardware capture, native/Python physical bounds and partial cartridge bank ranges. The draft is not yet reviewed for these edge cases.
4. Add DMG boot assets to packaged runtime inputs and expose explicit hardware selection through supported launchers/API. Current packaging has only the prior CGB boot asset; do not advertise packaged DMG support yet.
5. Implement and verify MBC2. The current adapter still declares ROM-only/MBC1/MBC3/MBC5. Handle 512-byte nibble RAM, mirrored addressing and CPU-versus-physical values; reconcile static `MBC2_RAM` mapping with runtime cartridge regions without renumbering persisted region IDs casually.
6. Run dedicated model/mapper tests, default CGB compatibility, pristine/instrumented comparisons and the plan's independent accuracy matrix, then refresh extracted Mac/Linux validation for affected artifacts.

M7 engineering began while the frozen M6 candidate was soaking. This is independent work, not a waiver of M6 or evidence that Release A contains the new hardware changes.

## Backend feasibility and next implementation

Read [backend decisions](backend-decisions.md). mGBA is pinned to `685023e05d90d87050fb357f46f7bd2d907083f5`; source/probe is retained under `/private/tmp/ghidraboy-integration-20260906/mgba-source` and `tools/backend_probes/`. Load, stepping, registers, same-PC/two-bank stops, physical WRAM watches, state restore, input and timebase have bounded probe evidence. There is no production adapter.

Inherit the actual mGBA CMake target's compile definitions/link dependencies: the generated flags header alone missed an ABI-affecting definition. Full-address-space `rawRead8` sweeps mutated serialized state; physical memory block reads preserved it. Use safe copied observations and explicit unavailable coverage. Complete M1 pause/timeout/conformance details, then implement M8 using the existing session, mappings, history, profiles and installer with capability-specific rejection.

Emulicious's pinned Linux DAP probe observed registers/hardware, a step from 0x0100 to 0x0101 and detach preserving the process. Coherent bulk memory, exact loaded-ROM identity and the complete Basic Debugging tier are not established. macOS ROM load stalled in native audio even after isolating first-run updates. It remains a future attachment option, not a second supported backend.

## Environment and commands

| Resource | Location / identity |
| --- | --- |
| Main work/evidence | `/private/tmp/ghidraboy-integration-20260906/`; `/Users/joshuahansen/dev/GhidraBoy/dist/integration-baseline-20260906/` |
| Linux work | `/private/tmp/ghidraboy-integration-linux-20260906/` |
| JDK 21 | `/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home` |
| Patched Ghidra 12.1.3 | `/tmp/ghidraboy-switch-recovery2-mac-final/distribution` |
| Disposable RMI Ghidra | `/private/tmp/ghidraboy-integration-20260906/rmi-install` |
| Gradle cache | `/tmp/ghidraboy-switch2-gradle-cache` |
| Local debugger Python | `/Users/joshuahansen/dev/GhidraBoy/debugger/.venv12/bin/python` (3.9); system test interpreter is 3.9.6 |
| Canonical dependencies | `/Users/joshuahansen/dev/GhidraBoy/tools/dependencies.json` |
| SameBoy | `208ba4afabffab9edde416f2dbb8ae459e34adb8`; local `debugger/.deps/SameBoy` |
| Native dependency | `12.1.3+ghidraboy.switch-recovery.2`; Mac executable SHA256 `5b736c3e9236667d35a732f226c99f0014736b9fe506147886a7f15ccb94939a` |

Core commands from the GhidraBoy root, after inspecting script arguments and selecting the intended artifact:

```sh
export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
export GHIDRA_INSTALL_DIR=/tmp/ghidraboy-switch-recovery2-mac-final/distribution
export GRADLE_USER_HOME=/tmp/ghidraboy-switch2-gradle-cache
./gradlew -PwithDebugger=true integrationArtifacts
bash debugger/scripts/build_native.sh
bash debugger/scripts/test_native.sh
```

Run commands separately or use explicit failure propagation. `debugger/scripts/build_extension.sh` wraps the root build; `debugger/scripts/package_candidate.py` consumes `build/integration/artifacts.json`. Extracted runtimes need `scripts/prepare_runtime.py` before direct tests, or use their `Validate.sh`. Use copied Ghidra installations and isolated homes; the existing-instance guard rejects overlapping validation in the same home. Review precise historical invocations in receipts before rebuilding or installing.

Linux recipes and identities are in `tools/linux/README.md` and `m3-linux-evidence.json`. Use Docker `--init`, `--platform linux/amd64`, a loopback hostname mapping, no network and an unprivileged UID for installed acceptance. Mount only the candidate, copied Ghidra, results and runner; no compiler/source mounts. The no-system-SDL runtime verifies the bundled fallback. Container results remain virtual-display/emulated-x86 evidence.

Gradle local sockets, process inspection, Docker and GUI execution may require sandbox escalation. A denied sandbox operation is not a source-test failure; retain actual outcomes. `PYTHONPYCACHEPREFIX` can point to a temporary directory for compile checks. Never restart a running tool merely because a wait yielded. Temporary evidence is local and may disappear; preserve the required checksummed artifacts before cleaning any work directory.

## Resume order and stopping conditions

1. Read the state snapshot, recheck Git/index and verify the completed soak's retained logs/results. Avoid mixing its frozen package with the draft M7 native binary.
2. Review/test the M6 assessor and finish matched metrics/resource/cleanup evidence. Review M7 separately; keep changesets small and attributable. Do not bulk-stage unrelated files or reset the pre-existing index.
3. Resolve remaining M0/M1 details and physical M4/M6 checks when the desktop is available. Refresh Release A evidence for an explicitly selected candidate.
4. Complete M7 model/mapper/accuracy work and a real M8 mGBA adapter with shared conformance; enforce capability limits in both API and UI.
5. Complete M9 integrated analysis/debug/research operations, then M10 combined packages, actual claimed-device acceptance and documented cutover. Publish/archive only when authorized and qualified.

No implementation, test rerun or commit was needed just to write this handoff. Documentation changes should be reviewed separately from the pre-existing staged Java harness and unstaged M7 edits. Missing physical acceptance or incomplete performance comparisons must remain visible; they do not justify claiming the migration complete.
